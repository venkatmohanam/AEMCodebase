(function ($, $document) {
    "use strict";



    $document.on("dialog-success", function() {
/*
        var currentDialog = Granite.author.DialogFrame.currentDialog;

        if(currentDialog.editable.name == 'footerlinks11'){

       	// $(window).adaptTo("foundation-ui").alert("Close", "Dialog closed, event [dialog-closed]");

            $.ajax({
              url: "/bin/aemmaster/footer",

            }).done(function( data ) {
                if ( console && console.log ) {
                  console.log( "Sample of data:", data);
                }
              });
            $(window).adaptTo("foundation-ui").alert("Footer links saved as json..");

       }*/
    });
})($, $(document));